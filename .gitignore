# Ignore VSCode settings
.vscode/

# --- Remove global image ignores ---
# (so that frames and images aren’t blocked)

# Keep frames folder and its contents
!frames/
!frames/**

# Keep essential files
!index.html
!zoom.png
!README.md
